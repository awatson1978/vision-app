
function SnellenExamViewModel() {
    /// <summary>
    /// A view model that renders an Eye Exam.
    /// </summary>

    this.template = "snellenExamView";
    //this.tweets = ko.observableArray(tweetViewModels);
}
function TokyoExamViewModel() {
    /// <summary>
    /// A view model that renders an Eye Exam.
    /// </summary>

    this.template = "tokyoExamView";
    //this.tweets = ko.observableArray(tweetViewModels);
}
function EExamViewModel() {
    /// <summary>
    /// A view model that renders an Eye Exam.
    /// </summary>

    this.template = "eExamView";
    //this.tweets = ko.observableArray(tweetViewModels);
}